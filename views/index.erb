<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Streaker</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
    <style>
      .container {
        max-width: 750px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="page-header">
        <h1><a href="/">Streaker</a>
          <small>... streaks of some sort.</small>
        </h1>
      </div>

      <% if @error %>
        <div class="alert alert-danger" role="alert"><%= @error %></div>
      <% end %>

      <p>
        <form method="post">
          <div class="input-group">
            <input class="form-control" id= "username" name="username"
              value="<%= @username %>"
              type="text" placeholder="GitHub username" autofocus>
            <span class="input-group-btn">
              <button type="submit" class="btn btn-default">Streak!</button>
            </span>
          </div>
        </form>
        <br>
      </p>

      <% if @result %>
        <div class="panel panel-default">
          <div class="panel-body">
            <h4>
              Streaks for <a href="https://github.com/<%= @username %>" target="_blank">@<%= @username %></a>:
            </h4>
            <h4>Longest streak: <%= streak_with_days(@result[:longest_streak]) %></h4>
            <h4>Current streak: <%= streak_with_days(@result[:current_streak]) %></h4>
          </div>
        </div>
      <% end %>

      <h4>Streaker defines a streak as:</h4>
      <blockquote>
        <p>“The number of consecutive days with <em>no</em> contributions”</p>
      </blockquote>

    </div>
  </body>
</html>
